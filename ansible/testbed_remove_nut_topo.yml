---
- name: Remove virtual NUT topology
  hosts: vm_host
  gather_facts: yes
  vars_files:
    - group_vars/all/creds.yml
    - group_vars/lab/secrets.yml
  roles:
    - role: testbed/nut-vtopo
      vars:
        vnut_action: teardown
